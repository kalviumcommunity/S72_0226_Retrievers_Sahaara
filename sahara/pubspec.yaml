name: sahara
description: "Sahara - Trusted Pet Discovery & Monitoring Platform"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.6.0
  firebase_messaging: ^14.7.10
  
  # State Management
  provider: ^6.1.1
  
  # Authentication
  google_sign_in: ^6.2.1
  
  # Image Handling
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1
  
  # Location Services (Temporarily disabled)
  # geolocator: ^11.0.0
  # geocoding: ^2.1.1
  
  # UI Components
  cupertino_icons: ^1.0.8
  
  # Utilities
  intl: ^0.19.0
  # uuid: ^4.3.3
  # timeago: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/images/
    - assets/icons/
